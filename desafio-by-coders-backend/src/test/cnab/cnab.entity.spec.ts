import { StoreTransactions } from '../../cnab/store-transactions.entity';

describe('CnabEntity', () => {
  it('should be defined', () => {
    expect(new StoreTransactions()).toBeDefined();
  });
});
